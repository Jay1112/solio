const initinalState = {
  appName: "Solio",
};

export const AppActions = {
  // will be added in future
};

export const appReducer = (state = initinalState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
